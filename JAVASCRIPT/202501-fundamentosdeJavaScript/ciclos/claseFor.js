/**
 * El ciclo for es una estructura de programación
 * que se utiliza para ejecutar un bloque de código
 * un número específico de veces, es uno de los ciclos
 * más utilizados en programación y se emplea para 
 * correr vectores, matrices y estructuras.
 */

let list = ["eat","sleep","code","repeat"];

for (let i = 0; i < list.length; i++){
    console.log(list[i]);
}